{
  "name": "land-game",
  "private": true,
  "version": "1.0.5",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp -r music dist/ && cp -r images dist/ && cp -r rules dist/ && cp -r changes dist/ && cp CHANGES.md dist/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "dependencies": {
    "three": "^0.160.0",
    "lil-gui": "^0.19.1"
  }
}